/**
 * Configuration
 * @module config
 */

//import NPMPackage from './package.json';

const config = {
  name: 'norton',
  title: 'Norton',
  description: 'Web App from React',
};

export default config;
